{
  "name": "07-restserver",
  "version": "1.0.0",
  "description": "A REST server is like a webserver, but apart from static content, it also contains access to our own JSON files and different endpoints from which we can GET and POST information.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1"
  }
}
